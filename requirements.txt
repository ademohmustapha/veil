# AURORA Security Platform — Requirements
# =========================================
# Core platform runs on Python 3.10+ with zero mandatory dependencies.
# All stdlib-only paths are fully functional and production-ready.
#
# Optional dependencies unlock performance and feature upgrades:

# ── ML-KEM Performance (Fix: ~8-15× NTT speedup) ──────────────────────────
# Without numpy: pure-Python NTT (keygen ~200ms, encap+decap ~350ms)
# With numpy:    vectorised NTT (keygen ~15ms,  encap+decap ~25ms)
# Set AURORA_KEM_BACKEND=numpy to force, =python to disable.
numpy>=1.24.0                     # optional: ML-KEM NTT acceleration

# ── Cryptography (hybrid AES-GCM, Ed25519 signatures, TLS) ────────────────
cryptography>=41.0.0              # optional: AES-256-GCM, Ed25519, TLS

# ── FIPS 140-3 Certified Binary Path ──────────────────────────────────────
# Set AURORA_FIPS_KEM=true to activate (requires liboqs-python installed).
# Without liboqs, AURORA uses the native NTT implementation (algorithmically
# correct per FIPS 203, but not a FIPS 140-3 validated module).
# liboqs-python                   # optional: FIPS 140-3 validated ML-KEM

# ── PostgreSQL storage backend ────────────────────────────────────────────
# psycopg2-binary>=2.9.6          # optional: PostgreSQL storage backend

# ── Argon2 password hashing ───────────────────────────────────────────────
argon2-cffi>=23.1.0               # optional but strongly recommended: Argon2id

# ── Development / Testing ─────────────────────────────────────────────────
pytest>=7.4.0                     # test runner
pytest-cov>=4.1.0                 # coverage reports
